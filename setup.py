#!/usr/bin/env python3

from distutils.core import setup

setup(name='turnkey-command',
    version='1.0',
    description='A highlevel interface for command execution',
    author='Stefan Davis',
    url='https://github.com/turnkeylinux/turnkey-command',
    packages=['command'])
